import { NextResponse } from "next/server";import { requirePermission } from "@/lib/auth";import { connectDb } from "@/lib/db";import { AuditLog } from "@/models";export async function GET(){const auth=await requirePermission("audit.read");if(!auth.ok)return auth.response;await connectDb();return NextResponse.json({items:await AuditLog.find().sort({createdAt:-1}).limit(300).lean()})}
