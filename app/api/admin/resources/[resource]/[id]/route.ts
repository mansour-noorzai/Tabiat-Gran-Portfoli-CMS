import { NextRequest,NextResponse } from "next/server";import { connectDb } from "@/lib/db";import { requirePermission } from "@/lib/auth";import { getResource } from "@/lib/resources";import { writeAudit } from "@/lib/audit";import { errorResponse } from "@/lib/http";import { can } from "@/lib/permissions";
function cleanBody(value:Record<string,unknown>){const body={...value};for(const k of ["_id","__v","createdAt","updatedAt"])delete body[k];return body}
export async function GET(_:NextRequest,ctx:{params:Promise<{resource:string;id:string}>}){const auth=await requirePermission("content.read");if(!auth.ok)return auth.response;const{resource,id}=await ctx.params,config=getResource(resource);if(!config)return NextResponse.json({error:"Unknown resource"},{status:404});try{await connectDb();const item=await config.model.findById(id).lean();return item?NextResponse.json({item}):NextResponse.json({error:"Not found"},{status:404})}catch(error){return errorResponse(error)}}
export async function PUT(req:NextRequest,ctx:{params:Promise<{resource:string;id:string}>}){const auth=await requirePermission("content.write");if(!auth.ok)return auth.response;const{resource,id}=await ctx.params,config=getResource(resource);if(!config)return NextResponse.json({error:"Unknown resource"},{status:404});try{const body=cleanBody(await req.json());if(body.status==="published"&&!can(auth.session.role,"content.publish"))return NextResponse.json({error:"Your role cannot publish content"},{status:403});await connectDb();const item=await config.model.findByIdAndUpdate(id,body,{new:true,runValidators:true});if(!item)return NextResponse.json({error:"Not found"},{status:404});await writeAudit(auth.session,"update",resource,id);return NextResponse.json({item})}catch(error){return errorResponse(error)}}
export async function DELETE(_:NextRequest,ctx:{params:Promise<{resource:string;id:string}>}){const auth=await requirePermission("content.write");if(!auth.ok)return auth.response;const{resource,id}=await ctx.params,config=getResource(resource);if(!config)return NextResponse.json({error:"Unknown resource"},{status:404});try{await connectDb();const item=await config.model.findByIdAndDelete(id);if(!item)return NextResponse.json({error:"Not found"},{status:404});await writeAudit(auth.session,"delete",resource,id);return NextResponse.json({ok:true})}catch(error){return errorResponse(error)}}
